# Reality Gap attachment read boundary

`RealityGapAttachmentReadContract` is the minimal immutable view exposed to
read-only consumers. It contains attachment identity, three independently
versioned artifact references, availability, and a UTC creation timestamp.

The package intentionally contains no evidence, candidates, projections,
mapping details, classification or metrics payloads, runtime services, API,
storage, production integration, or third-party dependencies.
