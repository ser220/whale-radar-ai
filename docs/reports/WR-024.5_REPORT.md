# WR-024.5 — Intelligence Contracts

## Status

Implementation and local verification complete. Feature branch awaiting Architecture Review before merge.

## Summary

Added a pure, immutable domain-contract layer for future Experts and
`MarketStateEngine`. The contracts are additive and are not integrated into the
existing production pipeline. WR-025 was not started.

## Architecture Review Documents

- [Accepted ADR](../architecture/adr/ADR-WR-024.5-intelligence-contracts.md)
- [Engineering specification](../specs/WR-024.5-intelligence-contracts.md)

## Repository Baseline

- Python version: 3.9.6
- Test framework: standard-library `unittest`; legacy tests are executable smoke scripts
- Starting branch: `main`
- Starting commit: `20a4528 WR-024.5 add intelligence contracts` (local commit created before adoption of this workflow)
- Remote baseline: `origin/main` at `7b7cb57 v0.5 Whale Radar AI foundation`
- Pre-existing uncommitted files: `docs/chat_archive/monitoring_crypto_whales/` (preserved and excluded from WR commits)

## Files Created

- `app/intelligence/__init__.py`
- `app/intelligence/contracts/__init__.py`
- `app/intelligence/contracts/enums.py`
- `app/intelligence/contracts/models.py`
- `app/intelligence/contracts/registry.py`
- `app/intelligence/contracts/_validation.py`
- `app/intelligence/contracts/README.md`
- `test_intelligence_contracts.py`
- `AGENTS.md`
- `docs/WORKFLOW.md`
- `docs/specs/README.md`
- `docs/architecture/adr/README.md`
- `docs/architecture/adr/ADR-WR-024.5-intelligence-contracts.md`
- `docs/specs/WR-024.5-intelligence-contracts.md`
- `docs/reports/WR-024.5_REPORT.md`

## Files Modified

No pre-existing tracked production files were modified.

## Public API

- `Direction`: `BULLISH`, `BEARISH`, `NEUTRAL`
- `TrendState`: `TREND`, `CORRECTION`, `REVERSAL`, `RANGE`, `UNKNOWN`
- `LifecycleState`: `WATCH`, `PREPARE`, `READY`, `ACTION`, `MANAGE`, `EXIT`
- Immutable `ExpertOpinion`, `MarketState`, and `DecisionState`
- `ExpertRegistry.register`, `get`, `get_all`, and `contains`

## Design Decisions

- Frozen dataclasses keep the domain layer independent from Pydantic and infrastructure.
- Python 3.9-compatible typing is used throughout.
- Mutable input collections are converted defensively; metadata is recursively frozen.
- Registry order follows deterministic dictionary insertion order; replacement retains position.

## Architecture Deviations

No deviations from the approved WR-024.5 architecture. `app/intelligence/__init__.py`
adds a convenience re-export without integrating the contracts into production.

## Validation Behaviour

Percentage-like numeric fields accept finite real values from 0 through 100,
inclusive. Booleans, non-numeric values, NaN, infinity, and out-of-range values
are rejected. Required text fields reject empty values. Naive timestamps are
rejected; aware timestamps are normalized to UTC.

## Serialization Behaviour

Each model supports `to_dict()` and `from_dict()`. Enum values serialize as
their public strings, timestamps serialize as ISO-8601 UTC values, and a
round-trip restores enum types, immutable collections, and aware UTC datetimes.

## Dependency Audit

`app/intelligence/contracts` imports only Python standard-library modules and
other modules inside the contracts package. It does not import Telegram,
FastAPI, persistence, repositories, the production pipeline, Arkham, source
adapters, funding adapters, or networking clients.

## Commands Executed

```text
git status --short
git branch --show-current
git remote -v
git log -5 --oneline
PYTHONPYCACHEPREFIX=/tmp/whale_radar_pycache ./venv/bin/python -m py_compile ...
./venv/bin/python -m unittest -v test_intelligence_contracts.py
./venv/bin/python test_core.py
./venv/bin/python test_filter.py
./venv/bin/python test_parser.py
rg import-boundary checks
git diff and status inspections
```

## Test Results

- Python 3.9.6 compile check: PASS
- Focused intelligence-contract tests: 14/14 PASS
- Safe scorer smoke test: PASS
- Safe filtering smoke test: PASS
- Safe Arkham parser smoke test: PASS
- Live Telegram/network tests: intentionally not run

## Production Pipeline Impact

None. The Arkham webhook, filtering, scoring, clustering, database, and Telegram
delivery files were not modified. Contracts are not imported by the pipeline.

## Git Information

- Feature branch: `wr-024-5-intelligence-contracts`
- Implementation commit: `20a4528`
- Workflow/report commit: `79b0f9e`
- Push result: SUCCESS — `git push -u origin wr-024-5-intelligence-contracts`
- Remote branch: `origin/wr-024-5-intelligence-contracts`

## Complete Branch Diff

The branch is additive relative to `origin/main`. Its complete tracked scope is:

```text
A  AGENTS.md
A  app/intelligence/__init__.py
A  app/intelligence/contracts/README.md
A  app/intelligence/contracts/__init__.py
A  app/intelligence/contracts/_validation.py
A  app/intelligence/contracts/enums.py
A  app/intelligence/contracts/models.py
A  app/intelligence/contracts/registry.py
A  docs/WORKFLOW.md
A  docs/architecture/adr/ADR-WR-024.5-intelligence-contracts.md
A  docs/architecture/adr/README.md
A  docs/reports/WR-024.5_REPORT.md
A  docs/specs/README.md
A  docs/specs/WR-024.5-intelligence-contracts.md
A  test_intelligence_contracts.py
```

No `app/main.py`, `app/engine/`, `app/sources/`, `app/storage/`, or
`app/telegram/` file appears in the branch diff. `docs/chat_archive/` is
untracked and is not part of the branch diff.

## Complete Branch Commit List

```text
WR-024.5 add architecture records
8a566d4 Update WR-024.5 report with push result
79b0f9e Add permanent engineering workflow
20a4528 WR-024.5 add intelligence contracts
```

The first line is the architecture-record commit created by this review update;
its final hash is reported by Git and in the Architecture Review response.

## Breaking Changes

None.

## Open Questions

No blocking questions. `ExpertRegistry.get()` intentionally uses normal mapping
semantics and raises `KeyError` for an unknown expert name. Probability fields
are validated individually; the specification does not require their sum to be
100.

## Recommended Next Step

Architecture Review of this feature branch, followed by explicit merge approval.
Do not begin WR-025 until it receives a separate approved task.

## Final Git Outputs

`git show --stat --oneline HEAD` immediately after the verified workflow commit
was pushed:

```text
79b0f9e Add permanent engineering workflow
 AGENTS.md                       |  19 +++++
 docs/WORKFLOW.md                |  51 +++++++++++++
 docs/architecture/adr/README.md |   4 +
 docs/reports/WR-024.5_REPORT.md | 161 ++++++++++++++++++++++++++++++++++++++++
 docs/specs/README.md            |   5 ++
 5 files changed, 240 insertions(+)
```

`git status --short` after the push:

```text
?? docs/chat_archive/
```

The remaining `docs/chat_archive/` subtree is unrelated user data and is
excluded from WR commits. The later report-only metadata commit records this
push result without altering implementation or workflow behaviour.
